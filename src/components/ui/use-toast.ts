import { useToast, toast } from "@/hooks/use-toast";
import { toast as sonnerToast } from "sonner";

// Re-export both toast systems for appropriate use cases
export { useToast, toast, sonnerToast };
